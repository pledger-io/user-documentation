allOf:
  - $ref: '#/components/schemas/paged-response'
  - type: object
    required:
      - content
    properties:
      content:
        type: array
        items: { $ref: '#/components/schemas/transaction-response' }
