parameters:
  - name: type
    in: path
    required: true
    schema:
      type: string
      enum: [ daily, monthly ]
post:
  operationId: computeBalanceGroupedByDate
  tags: [ statistics-balance ]
  summary: Compute date partitioned balance
  description: >
    Computes the balance grouping it by date as indicated in the `type`.
  requestBody:
    content:
      application/json:
        schema: { $ref: '../openapi.yml#/components/schemas/balance-request' }
  responses:
    "200":
      description: The balance for the given filters
      content:
        application/json:
          schema:
            type: array
            items: { $ref: '../openapi.yml#/components/schemas/balance-dated-response' }
    "401": { $ref: '../openapi.yml#/components/responses/401' }
    "403": { $ref: '../openapi.yml#/components/responses/403' }
