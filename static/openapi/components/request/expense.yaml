type: object
required:
  - name
  - amount
properties:
  id:
    type: integer
    format: int64
    description: >
      The unique identifier for the expense.
      Only fill when updating an existing expense.
  name:
    type: string
    minLength: 1
  amount:
    type: number
    format: double
    minimum: 0
