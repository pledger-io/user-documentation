post:
  operationId: uploadFile
  tags: [ files ]
  summary: Create file
  requestBody:
    content:
      'multipart/form-data':
        schema:
          type: object
          properties:
            upload:
              type: string
              format: binary
    required: true
  responses:
    "201":
      description: The file code for the created file
      content:
        application/json:
          schema: { $ref: "../openapi.yml#/components/schemas/file-response" }
    "401": { $ref: '../openapi.yml#/components/responses/401' }
    "403": { $ref: '../openapi.yml#/components/responses/403' }
