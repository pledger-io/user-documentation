post:
  operationId: createUser
  tags: [ security ]
  security: []
  summary: Create user
  requestBody:
    content:
      application/json:
        schema: { $ref: '../openapi.yml#/components/schemas/user-request' }
  responses:
    "204":
      description: Confirmation the account was created
    "400": { $ref: '../openapi.yml#/components/responses/400' }
